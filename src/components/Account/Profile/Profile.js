import React from 'react';
import { Box,makeStyles,createStyles, Container,Paper } from '@material-ui/core'


const Profile = ()=> {
    const classes = useStyles();
return(
    <Box component={Paper}>
        <Container>

        </Container>
    </Box>
)
};

const useStyles = makeStyles(theme =>{
    createStyles({

    })
})

export default Profile;